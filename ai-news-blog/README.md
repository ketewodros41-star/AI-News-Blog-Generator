
# 🤖 AI News Blog Generator

Production-ready Next.js 14 app that fetches live news and generates AI summaries.

## Setup
1. Copy `.env.example` → `.env`
2. Add API keys
3. `npm install && npm run dev`

## Docker
docker build -t ai-news-blog .
docker run -p 3000:3000 ai-news-blog
